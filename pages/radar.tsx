import * as React from 'react';
import MainLayout from '../layouts/main';

interface IRadarProps {}

const Radar: React.FC<IRadarProps> = props => {
    return (
        <MainLayout>
            <div>Radar</div>
        </MainLayout>
    );
};

export default Radar;
